spring:
  profiles:
    group:
      default: real-db
      test: test-db

logging:
  level:
    root: info

management:
  #endpoints.web.exposure.include: caches, health, metrics
  endpoints.web.exposure.include: '*'